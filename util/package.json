{
  "name": "typing-aoc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsup index.ts --format esm,cjs && tsc index.ts --declaration --emitDeclarationOnly --outDir dist",
    "format": "pnpm exec prettier --write .",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "3.0.2",
    "typescript": "^5.3.2",
    "vitest": "^0.32.4"
  },
  "dependencies": {
    "tsup": "^8.0.1"
  }
}
